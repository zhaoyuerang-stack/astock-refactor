# Quant research engine + product API (Phase 0)
# 运行时 direct deps 与 pyproject.toml 保持一致;推荐先建 venv 再装:
#   python3 -m pip install -e '.[dev]'
# 若必须用系统 Python(外部管理),则:
#   python3 -m pip install --break-system-packages -r requirements.txt

# --- 引擎(已在用)---
pandas==3.0.3
numpy==2.2.6
pyarrow==24.0.0
scipy==1.17.1

# --- 产品 API 层(Phase 0 新增)---
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4

# --- 即席查询 / 数据 QA(Phase 2;可选,services 内优雅降级)---
duckdb==1.5.3

# --- 数据源 ---
# baostock: ETF/股票日线备用源(akshare 东财挂/被代理时自动 fallback)
# 完全免费,无 token,数据覆盖 2006-至今,支持后复权/不复权
baostock==0.9.2
